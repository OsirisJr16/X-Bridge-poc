# Runtime dependencies are declared in pyproject.toml.
# This file exists for `pip install -r requirements.txt` workflows and mirrors it.
#
# Preferred install (editable, with dev extras):
#   pip install -e ".[dev]"

numpy>=1.26
pydantic>=2.7
scikit-learn>=1.5
sentence-transformers>=3.0
torch>=2.3
